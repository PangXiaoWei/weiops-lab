{
  "name": "weiops-lab",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8767",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8767",
    "check-audio": "node scripts/check-audio.mjs",
    "generate-audio": "node scripts/generate-audio.mjs",
    "generate-audio:test": "node scripts/generate-audio.mjs --limit=10"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
